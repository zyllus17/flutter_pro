//An example test to see if the [launch.json] is working properly or not
import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    "1 + 1 == 2",
    () async {
      expect(1 + 1 == 2, true);
    },
  );
}
